services:
  livekit:
    image: livekit/livekit-server:latest
    command: --dev
    ports:
      - "7880:7880"   # HTTP / WebSocket
      - "7881:7881"   # RTC over TCP
      - "7882:7882/udp" # RTC over UDP (음성 패킷)
